---
---
<div class="offline-status" aria-live="polite" data-role="offline-status">
  <p>Aplikasi ini dapat berjalan tanpa koneksi internet (Offline First).</p>
</div>

<style>
  .offline-status {
    background-color: var(--color-ink);
    color: var(--color-chalk);
    border: 1px dashed var(--color-chalk);
    padding: 0.5rem 1rem;
    margin-bottom: 2rem;
    display: inline-block;
    align-self: flex-start;
    font-family: var(--font-ui);
    font-size: 0.9rem;
  }
</style>
